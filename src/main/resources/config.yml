# Max counted kills from the same killer -> same victim within the rolling window.
max-kills-per-target-window: 2

# Rolling window in minutes.
target-window-minutes: 30

# SQLite file location relative to the plugin folder.
# Example result: plugins/KillStatistics/stats.db
database:
  file: stats.db
